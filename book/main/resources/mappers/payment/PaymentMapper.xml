<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tccins.template.payment.mapper.PaymentMapper">
    <!-- 결제 상세정보 조회 -->
    <select id="selectPayInfoDetail" parameterType="com.tccins.template.payment.PaymentVO" resultType="com.tccins.template.payment.PaymentVO">
		SELECT
			 FILEDSTRING
			,FILEDDECIMAL
			,FILEDDATE
			,FILEDDOUBLE
			,FILEDINTEGER
			,ORDERNO
			,FILEDSTRING2
		FROM
			PAYMENT_INFO
		WHERE ORDERNO = #{orderNo}
		LIMIT 1
    </select>

    <!-- 결제 목록 조회 -->
    <select id="selectPayInfoList" parameterType="com.tccins.template.payment.PaymentVO" resultType="com.tccins.template.payment.PaymentVO">
		SELECT
			 FILEDSTRING
			,FILEDDECIMAL
			,FILEDDATE
			,FILEDDOUBLE
			,FILEDINTEGER
			,ORDERNO
			,FILEDSTRING2
		FROM
			PAYMENT_INFO
		WHERE ORDERNO = #{orderNo}
	</select>
</mapper>